import { Routes } from '@angular/router';
import { WorkerEnrollmentComponent } from './worker-enrollment/worker-enrollment.component';

export const routes: Routes = [
  {
    path: '',
    component: WorkerEnrollmentComponent
  }
];
